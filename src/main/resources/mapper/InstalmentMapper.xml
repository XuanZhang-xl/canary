<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xl.canary.mapper.LoanInstalmentMapper">

    <select id="getByInstalmentId" resultType="com.xl.canary.entity.LoanInstalmentEntity">
        select * from t_canary_instalment WHERE instalment_id = #{instalmentId}
    </select>

</mapper>